# back-end-Geoprofs-LJ3

For realese change 'allowed_origins' in back-end-geoprofs\config\cors.php to the url of the front-end.

if logging in doesn't work try 
php artisan config:clear 
php artisan cache:clear